# ROS 2

## Topic Discovery Between Host and Docker

If topics published inside a Docker container are not visible on the host machine, check that both sides share the same `ROS_DOMAIN_ID`.

```bash
# Check current domain ID
echo $ROS_DOMAIN_ID

# Set domain ID (must match on both host and container)
export ROS_DOMAIN_ID=<your_id>
```
